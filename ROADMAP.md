# DesktopAgent 演进路线图

三档定位：同一套主干，按档位替换/增强 `core/` 与工具模块，独立迭代升级。

## 架构分层

```
gui.py                界面（个人版=本机 GUI；平台版=可换 Web 控制台）
agent_loop.py         agent 主干（LLM 循环、工具调度，档位无关）
agent_vision.py       工具包：视觉/窗口/UIA/剪贴板（按档位增工具）
core/                 企业化基础设施（按档位替换实现，接口不变）
  ├ audit.py          审计：个人=本地哈希链 JSONL；企业=集中上报；平台=审计中台
  ├ guard.py          危险操作规则库：数据驱动，可独立热更新
  ├ approval.py       操作确认：个人=GUI 弹窗；企业=审批流；平台=策略引擎
  ├ history.py        任务历史：个人=本地 JSONL；企业=数据库；平台=任务中心
  └ paths.py          数据目录
```

## 能力矩阵

| 能力 | 个人版（当前档） | 企业版 | 大型企业及平台 |
|---|---|---|---|
| 审计日志 | ✅ 本地哈希链 JSONL | 集中上报、防篡改存储 | 审计中台、合规导出 |
| 危险操作 | ✅ 规则库+GUI 弹窗确认 | 审批流（多级审批） | 策略引擎、按组织/角色差异化 |
| 任务历史 | ✅ 本地持久化+回看 | 数据库、按人检索 | 任务中心、成功率看板 |
| 认证权限 | 本机单用户 | 账号登录、角色权限 | SSO/AD、细粒度授权 |
| 触发方式 | ✅ 手动、定时（每天/每周/间隔） | 定时/Webhook/文件监听 | 事件总线、流程编排 |
| 可靠性 | ✅ 停止、重试（指数退避）、动作后校验 | 断点续跑、动作后校验 | 分布式队列、SLA 监控 |
| 模型 | ✅ 本地 Ollama，设置页热切换 | 混合路由（本地+云端） | 模型网关、按任务选型、微调 |
| 感知 | 截屏+UIA 控件（已有） | +UIA 文本读取、OCR | 浏览器 DOM、Office COM、远程桌面 |
| 集成 | 剪贴板（已有） | 数据库/API/邮件/OA | 全家桶+自定义连接器市场 |
| 管控 | 单机 | 集中部署升级 | 多机集群、License、租户 |

## 当前档位：个人版（功能已齐）

已交付：
- 安全：审计日志（哈希链防篡改）、危险操作确认（规则库 + GUI 弹窗）
- 可靠性：任务重试（指数退避 1s/2s/4s）、动作后校验（开应用验窗口 / 点击验屏幕变化 /
  写剪贴板验内容 / 文件验存在）
- 自动化：定时触发（每天 / 每周 / 间隔，JSON 持久化，重启不重复触发）
- 模型：设置页切换（读取 Ollama 模型列表，保存后热切换立即生效）
- 体验：系统托盘（关窗最小化、右键显示/退出、状态提示）、任务历史回看、执行中可停止

个人版可选增强：动作后 OCR 校验、任务导入导出、多显示器坐标适配。
下一步档位：企业版——账号登录、集中上报审计、审批流、混合路由（本地执行 + 云端规划）、
重试/断点续跑策略服务化、数据库任务历史。
